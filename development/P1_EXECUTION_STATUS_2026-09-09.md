# 栖语 P1 执行状态（2026-09-09）

本台账以当前代码、当前本机验证与可追溯报告为准。“开发完成”不等于“生产上线”；真实账号体系、支付、第三方年龄增强核验、生产数据删除、审核员生产身份和外部 Push 仍受生产门禁约束。

## 总览

| P1 项目 | 当前结论 | 已完成开发与验证 | 尚未完成 / 外部阻断 |
|---|---|---|---|
| Qwen + 腾讯 ASR/TTS + 混元同一浏览器 E2E | 开发验收完成 | 同一 Edge 会话完成真实 Qwen SSE、腾讯 TTS、以刚生成私有音频执行腾讯 ASR、受控参考图 IMS/权利审核、混元生图、私有 COS 鉴权读取；13/13 PASS，控制台错误 0、意外 5xx 0 | 账户和权益仍为内存开发态；真实支付、第三方年龄核验、生产删除不在该链路 |
| 42 个原型状态自动化 | 静态门禁完成，运行态部分完成 | 42/42 状态 URL 可达、标题/状态 ID/结构渲染正确，控制台错误 0；运行态另覆盖记忆拒绝后 30 秒撤销、TTS 降级、通知入口等，默认浏览器链路 10/10 | 42 状态门禁验证的是交付原型，不等于 42 个 Web/API 业务状态均已动态驱动；年龄第三方材料、真实生产删除等仍无外部系统证据 |
| 真实短信发送 | 接线完成，真实送达阻塞 | 腾讯 SMS Adapter、随机 6 位码、频控、错码熔断、拒绝映射、验收脚本和容器变量注入已完成 | 本机未配置已审核签名、模板、SDK App ID 与本人验收手机号，不能产生真实送达证据 |
| 通知调度 | 站内调度完成 | 试用临近到期、续费提醒/完成、退款状态、删除完成、安全升级均有确定性调度、去重键、PG 持久化与 Worker 周期扫描 | 尚未接入真实 Push/短信/邮件投递供应商；Push 当前仅有隐私载荷策略与 Service Worker 通用展示 |
| 独立运营台 | 开发台完成；审核员身份/RBAC/双人审批/审计已落地并经容器真实验收 | 独立 4311 进程；账号登录（scrypt + 失败锁定 + 会话吊销）+ TOTP MFA + RBAC 三角色矩阵 + 双人审批（人格 stable 发布、订阅人工撤销）+ 追加写运营审计 + 年龄材料按需解密（AES-GCM 先留痕后解密）；PG 迁移 055（qiyu_reviewer RLS + 审计触发器拒改）；容器内 12/12 真实验收（scripts/verify-reviewer-pg-session.js）；写操作浏览器 E2E 全链路 12 步通过（登录失败→RBAC 拒绝→MFA→双人审批→审计可见），控制台 JS 错误 0 | 单人项目的角色分离是逻辑上的（双人审批要求不同账号，但同一人可持多账号，不构成真实组织职责分离）；审计表主/超主仍可 TRUNCATE（PG 无该事件触发器，应用角色已无权限，彻底不可变需外部 WORM，列生产加固）；投诉/安全/删除异常保持只读 |
| 成本、对账、分位看板与告警 | 工具与部署件完成，真实结算未完成 | 费率支持生效时间调价与阶梯价（无时间戳埋点不猜价）；对账支持日/月分桶与调整项（优惠券/代金券/退款/税差，白名单校验）；Prometheus+Alertmanager+Grafana+postgres-exporter compose 覆盖层、告警规则、看板（分位/失败率/单位成本/队列/PG 实例/审计行数）与个人 Push/邮件渠道配置模板交付，compose config 校验通过 | 未导入真实供应商周期账单（合成样例验证程序，不能声称真实金额已核对）；本机磁盘受限未拉镜像，监控栈尚未实际运行；告警渠道未实测触发 |
| Web/App 发布、缓存、Push、CI/CD、灰度、灾备 | Web 工程化部分完成 | PWA Manifest/静态壳缓存、AES-GCM 会话令牌缓存、通用 Push 隐私载荷、GitHub CI、人工真实供应商门禁、canary Compose 配置均已落盘；canary 合并配置通过校验 | `NODE_ENV=production` 仍由 `PRODUCTION_RUNTIME_NOT_WIRED` 主动拒绝；无原生 App 构建/签名/商店发布、真实 Push、远端 CI 运行证据、真实流量灰度控制面和真实备份介质恢复演练 |

## 当前复验证据

- 默认 Web/API 浏览器链路：10/10 PASS；新增“记忆拒绝后撤销”为服务端状态恢复；控制台错误 0，意外 5xx 0。报告：`development/eval/browser-e2e-2026-09-09.md`。
- 真实供应商单浏览器链路：13/13 PASS；报告：`development/eval/browser-e2e-2026-09-08.md`（报告内标记真实 Qwen + 腾讯媒体模式）。
- 原型状态：42/42 PASS；报告：`development/eval/prototype-42-state-browser-e2e-2026-09-08.md`。
- 独立运营台：年龄复核、投诉、安全事件、删除异常四类浏览器验收通过；控制台错误 0。
- API 单元/契约回归：269/269 PASS（本轮代码完成后运行）。
- Docker：PostgreSQL、API、Worker 已运行；迁移账本 54；最新为 `054_memory_rejection_undo.sql`。
- canary Compose：`docker compose -f deploy/docker-compose.yml -f deploy/docker-compose.canary.yml --profile canary config --quiet` 通过。

## 下一步按阻断解除顺序

1. 取得腾讯云已审核短信签名/模板/SDK App ID，并由开发者提供本人测试号码后，执行一次真实短信送达验收。
2. 审核员身份体系已落地（账号/MFA/RBAC/双人审批/审计，容器内 12/12 验收）；生产启用时以 `scripts/seed-ops-reviewer.js` 建首账号、配置 `QIYU_OPS_MATERIAL_KEY`，并停用静态开发 token。
3. 导出同周期供应商账单和实际费率，执行账单对账（`reconcile-provider-bill.js --bucket=month` + 调整项）；磁盘允许后拉起监控栈并触发一次测试告警，才能声称线上告警已运行。
4. 完成生产账号、支付、第三方年龄断言与供应商删除证明后，才能解除生产启动门禁。
5. 选择 App 技术栈与 Push 供应商，完成原生构建签名、真实灰度流量、回滚和备份恢复演练。
