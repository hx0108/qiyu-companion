# 栖语 M1 实施状态矩阵

更新时间：2026-09-05（含当日第二批开发、审查修复、世界状态快照、会话控制/反馈、OC/参考图隔离审核、模型输出 Schema/审核、AI-03/AI-08 模型门禁、开发期调用指标、TTS 音色溯源、审核撤销与物理清理任务边界；本地回归测试 158/158 通过，见 `V1_GAP_ANALYSIS.md`）。此文档只记录当前工作区的代码与已运行测试；`本地开发`、`Adapter 已定义`和`真实供应商验证`不得互相替代。

| PRD 主链 | 当前状态 | 当前证据 | 仍缺失的上线证据 |
|---|---|---|---|
| 必要 AI 身份告知与年龄基础准入 | 本地开发已验证 | 服务端告知回执、年龄声明与访问控制；API 测试 | 真实注册、法务确认的联系人规则与外部年龄核验 |
| 风险触发年龄增强核验/申诉 | 本地状态机已验证 | DOB 变更/无效声明/申诉进入 `AGE_REVIEW` 并阻断互动 | 腾讯云/阿里云最小断言、签名回调、重放防护、人工申诉 SOP |
| 文字对话与安全 | 本地与 Qwen Adapter 已接线 | 确定性 R2、TMS fail-closed、候选记忆确认；真实 Qwen 路径要求 `companion_reply.v1` JSON Schema，一次格式纠错重试后仍失败则用固定降级文本且不建候选。文本审核按 `INPUT`（模型前）、`OUTPUT`（模型终稿持久化/候选/SSE 前）和 `TTS_OUTPUT`（合成前）显式分流。模型输出未通过时只返回固定审核说明，原始回复不展示、不落库、不建候选，并释放本轮额度；读取中的短期世界状态进入模型上下文，模型候选不能直接写状态；会话可暂停/恢复，反馈与模型/状态快照关联而不自动写记忆；既有会话在年龄状态转 `AGE_REVIEW` 后，普通消息在审核器/模型前被确定性阻断；人格/关系资产被标为不可信数据并转义后才进入 Qwen 系统段。AI-08 固定攻击集覆盖人格、确认记忆、历史和当前输入四种来源；模型声称绕过安全/年龄/权限、开放未成年人互动、跨账户导出或直接改写状态时，确定性输出门禁不展示、不落库、不建候选且释放额度 | 供应商 token 级流式、逐段输出审核、扩展攻击集与真实模型红队/人工评测、生产审计与运营值班 |
| 关系记忆/时间线 | 本地开发已验证 | 候选—确认/编辑—资产—撤销/会话删除—导出测试 | pgvector、冲突检测、跨设备与生产删除编排 |
| 角色世界状态 | 本地开发与 PostgreSQL 契约已验证 | `GET/PATCH/reset`、字段白名单、乐观锁、TTL、`USER_RESET` 追加事件；迁移 017 的账户 RLS；对话回复、TTS 和图片创建均冻结 `world_state_id + state_version`，迁移 018/019 持久化绑定 | 真实数据库迁移、角色模板/服装权利审核、确定性事件规则与图片一致性评测 |
| 用户语音输入 | 腾讯 ASR 本地真实链路已验证 | 受控短音频、确认后删除原始音频、前端修订再发送 | 对象存储、音频审核、24 小时 TTL、配额与队列 |
| 角色异步语音 | TTS 适配器与本地链路已验证；真实启动待完整配置 | TMS 前置、私有 MP3、鉴权播放、单项删除、ID3v2.3 AIGC 标识、开发额度账本；TTS 继承原助手回复的世界状态快照而非请求时当前状态。2026-09-05 真实启动在服务端门禁处停止：外部 `.env` 缺少 `TENCENT_TTS_VOICE_VERSION`，未调用腾讯 TTS | 补齐固定授权音色的版本、授权记录和审核记录后进行真实 TTS/COS 媒体闭环；对象存储/CDN、供应商精确时长、生命周期 Worker |
| 受控情境图片 | 本地链路、配置启动、前端浏览器流及云端前半链路已验证 | 混元提交/查询 Adapter、IMS 前后审核、固定 `qiyu/images/` 私有 COS 读写/短签名 URL、受控情境契约、鉴权内容代理与失败即删对象；参考图在 IMS PASS 后仍进入私有 `REVIEW_REQUIRED` 权利审核，只有关联 `APPROVED` 审核记录才可在权益预留、混元调用前通过门禁；迁移 022 持久化审核关联。浏览器验收显示待审状态、刷新/申诉入口且不进入生图页。本地图片模式健康检查通过。2026-09-04 两次非真人运行时测试图均完成 COS 上传、IMS 前审并删除参考对象；混元提交阶段返回 `AuthFailure.UnauthorizedOperation`，未获得任务 ID 或保存结果图。已完成本地图片任务的额度预留—成功结算—失败返还测试接线，但默认开发图片模式不计费 | 为 `.env` 所用 SecretId 的 CAM 身份授予 `SubmitHunyuanImageJob` 与 `QueryHunyuanImageJob` 后，用合规测试立绘复测完整闭环；独立审核员角色/队列及审核撤销后的 Outbox 停止/删除编排；实际应用 PostgreSQL 迁移；角色身份一致性、真实已验签支付事件接入、生命周期/删除回执与生产审计 |
| OC 导入与内容权利 | 本地隔离接收与审核路由已验证 | `POST /characters/imports` 仅在原创/授权声明后保存限期隔离原文，提取受控人格候选并默认 `REVIEW_REQUIRED`；风险码仅用于路由，不能替代判断；审核记录查询与申诉可用；浏览器已验收 OC 提交后只显示待审、刷新与申诉，不出现创建角色入口；迁移 021 将原文存为 `bytea`（开发隔离，**不是加密**）并限制应用角色只能查询/创建待审记录，不能批准 | 独立审核员角色/队列、证据收集与复核 SOP；参考图/声音同一权利流程；真正的加密密钥管理与留存清理 Worker |
| 数据权利 | 本地开发部分已验证 | 关系档案 JSON、30/90 天原始互动保留、单项资产删除、会话级撤销、导出隔离；持久化路径按策略写入原始消息到期时间并清理过期原始消息 | 生产导出任务、加密下载、对象存储/备份/供应商删除回执、注销 |
| 订阅与配额 | 本地开发模拟支付入口；账本、订阅状态机和持久化迁移已实现 | ¥39/¥108、默认非自动续费与不关系绑架说明；`GRANT → RESERVE → COMMIT/RELEASE` 追加式账本、余额计算、幂等与余额不足/重复结算测试。`checkout-sessions` 与 HMAC 验签回调仅用于 `DEVELOPMENT_SIMULATED`，不是真实收款；真实支付宝/微信仍关闭。每日普通对话 100 轮/32 万输入 Token 采用预留—提交—释放，PostgreSQL 路径在消息请求内锁定当日行。 | 支付商户、真实签名回调、渠道事件映射、退款/宽限/恢复购买/对账；应用 PostgreSQL 迁移并将已验签渠道事件接入额度服务 |
| 开发期调用指标 | 本地开发已验证 | 成功对话记录供应商、模型版本、输入/输出 Token、延迟和结果状态；账户只读查询且不含正文，跨账户为空 | PostgreSQL 持久化、媒体/审核/重试指标、实际金额口径、P50/P90/P99 看板与访问审计 |
| PostgreSQL 与生产运行时 | 基础骨架与局部迁移 | RLS 单测、迁移文件、按账户 30/90 天映射的会话/消息写入与原始消息删除测试；迁移 007 覆盖图片任务断点续跑、参考图确认和审核状态；迁移 008 提供账户级 RLS、追加式权益账本和禁止修改/删除触发器；迁移 010 提供订阅、订单、支付事件及隔离事件状态，且不保存支付原始回调；迁移 011 仅存媒体任务选择的订阅周期权益标识；迁移 015 持久化每日对话计量，迁移 016 显式限制开发模拟支付渠道，迁移 017/018/019 增加世界状态审计、媒体与消息快照绑定，迁移 020 增加会话暂停与级联删除的反馈记录，迁移 021 隔离 OC 原文并记录仅可由独立审核角色决策的权利审核，迁移 022 关联参考图与审核记录；生产启动 fail-closed | 真实数据库迁移、队列、对象存储、监控、备份与灾备演练 |

> 2026-09-04 补充：参考图通过 `GET /characters/{id}/reference-images` 在浏览器刷新后恢复安全元数据和权利审核状态，不返回对象键、签名 URL 或原始图片。腾讯 TTS 现将服务端固定的 `voice_id / voice_version / authorization_record_id / rights_review_id` 与 `APPROVED` 状态冻结到每个任务，迁移 023 持久化且不对历史任务伪造批准记录；真实腾讯适配器缺少任一服务端记录会拒绝启动。迁移 024 引入 `qiyu_reviewer`、审核员身份映射和受控决策函数：应用角色无法批准，审核决策会更新素材可用状态并写审计/Outbox。迁移 025 只允许独立审核员撤销既有批准：OC 立即撤销，参考图及由其生成、仍在线的图片立即标记为不可访问，并写入要求物理清理的 Outbox 事件。迁移 026 和独立清理进程将参考图撤销事件变为带租约、失败退避与删除清单回执的 COS 删除任务；它只完成自身任务，不篡改通用 Outbox 的 `published_at`。真实 COS 删除、供应商回执字段与真实数据库迁移尚未运行。上述能力均仍需运营身份配置和外部授权证据，不能视为已完成生产审核运营。

> 同日真实模型冒烟：使用现有本地凭据启动临时内存 API，完成必要告知、年龄准入、角色与会话创建后，对一条非敏感普通消息调用 Qwen；响应为 `provider=qwen`、`ai_generated=true`、含候选记忆，未触发 `qwen-schema-fallback`。该一次性验证证明当前凭据下的正常 Schema 路径可用；不证明长期 Schema 服从率、延迟分位数、安全效果或生产可用性。

> 同日 Qwen+TMS 联调：首次普通文本被审核策略保守替换，第二条最短普通文本通过输入及输出审核并走 `provider=qwen` 正常路径。期间发现并修复 TMS 的通用 `TENCENT_REGION` 被误判为图片链路启用的启动缺陷；图片门禁现只由图片专属配置触发。该双样本不构成审核误判率、策略质量或生产运营结论。

## 下一优先级

1. 为 `.env` 所用 SecretId 的 CAM 身份补齐混元 `SubmitHunyuanImageJob` / `QueryHunyuanImageJob` 权限；用合规、非敏感的角色测试立绘复测云端图片闭环，再补齐身份一致性、实际应用 PostgreSQL 迁移与生命周期/删除回执。
2. 支付订单—签名回调—渠道沙箱验收，只有具备商户主体、渠道产品与密钥后才连接支付宝/微信支付；未知或未验签事件必须进入隔离，且不得发放迁移 008 中的权益。
3. 年龄供应商最小断言回调与人工申诉队列，未通过前保持内部环境。
4. PostgreSQL/对象存储/队列与 30/90 天、24 小时删除 Worker；不得把当前本地内存删除描述为生产删除。
5. 应用迁移 024/025/026 前，按 [内容权利审核员部署与验收](CONTENT_RIGHTS_REVIEWER_DEPLOYMENT.md) 配置独立审核员和清理 Worker 数据库身份；先验证应用角色不能批准/撤销，再验证审核决策、撤销、清理租约与 Outbox 订阅互不抢占。真实 COS 删除回执仍需在云端验收。

## 外部封测门禁

当前不满足 H3-FB。任何外部用户互动前，必须补齐适用义务书面结论、年龄/安全/危机/数据权利/申诉能力、供应商合同和相应端到端演练。
